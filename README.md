# Damage Report Form

The "Damage Report Form" project is a small Angular application for submitting vehicle damage reports.

## Overview

A minimal frontend application demonstrating a damage report form (source files are located in `src/app/form-szkoda`).

## Requirements

- Node.js (>=14)
- npm (>=6)

## Technologies

- Angular (version defined in `angular.json`)
- TypeScript
- HTML / CSS
- npm (package management)

## Installation

1. Install dependencies:

```bash
npm install
```

2. Start the development server:

```bash
npm start
```

3. Run tests:

```bash
npm test
```

## Project Structure

- `angular.json` — Angular project configuration
- `src/` — application source code
  - `app/` — main application files
    - `form-szkoda/` — damage form component

## Next Steps

- Add detailed API/backend documentation if needed.
- Expand component documentation and unit tests.

## Author

- maxsoft

## Version

- 1.0.0

## License

- MIT — see the [LICENSE](LICENSE) file in the repository.